// Temporary route to preview the Loading screen without depending on
// cache/pipeline timing. Delete once verified live.
import Loading from '../loading';
export default function LoadingPreviewPage() {
  return <Loading />;
}
